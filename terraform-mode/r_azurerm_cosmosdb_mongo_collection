# -*- mode: snippet -*-
# name: r_azurerm_cosmosdb_mongo_collection
# key: r_azurerm_cosmosdb_mongo_collection
# --

resource "azurerm_cosmosdb_mongo_collection" "${1:this}" {
    account_name = "${2:account_name}"
    database_name = "${3:database_name}"
    name = "${4:name}"
    resource_group_name = "${5:resource_group_name}"
}