# -*- mode: snippet -*-
# name: r_infoblox_a_record
# key: r_infoblox_a_record
# --

resource "infoblox_a_record" "${1:this}" {
    cidr = "${2:cidr}"
    ip_addr = "${3:ip_addr}"
    tenant_id = "${4:tenant_id}"
    vm_name = "${5:vm_name}"
    zone = "${6:zone}"
}