# -*- mode: snippet -*-
# name: r_aws_opsworks_custom_layer
# key: r_aws_opsworks_custom_layer
# --

resource "aws_opsworks_custom_layer" "${1:this}" {
    name = "${2:name}"
    short_name = "${3:short_name}"
    stack_id = "${4:stack_id}"
}