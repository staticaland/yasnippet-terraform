# -*- mode: snippet -*-
# name: r_datadog_integration_gcp
# key: r_datadog_integration_gcp
# --

resource "datadog_integration_gcp" "${1:this}" {
    client_email = "${2:client_email}"
    client_id = "${3:client_id}"
    private_key = "${4:private_key}"
    private_key_id = "${5:private_key_id}"
    project_id = "${6:project_id}"
}