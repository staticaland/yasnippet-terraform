# -*- mode: snippet -*-
# name: r_rancher_registry_credential
# key: r_rancher_registry_credential
# --

resource "rancher_registry_credential" "${1:this}" {
    name = "${2:name}"
    public_value = "${3:public_value}"
    registry_id = "${4:registry_id}"
    secret_value = "${5:secret_value}"
}