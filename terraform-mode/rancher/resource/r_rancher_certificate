# -*- mode: snippet -*-
# name: r_rancher_certificate
# key: r_rancher_certificate
# --

resource "rancher_certificate" "${1:this}" {
    cert = "${2:cert}"
    environment_id = "${3:environment_id}"
    key = "${4:key}"
    name = "${5:name}"
}