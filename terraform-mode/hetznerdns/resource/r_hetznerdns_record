# -*- mode: snippet -*-
# name: r_hetznerdns_record
# key: r_hetznerdns_record
# --

resource "hetznerdns_record" "${1:this}" {
    name = "${2:name}"
    type = "${3:type}"
    value = "${4:value}"
    zone_id = "${5:zone_id}"
}