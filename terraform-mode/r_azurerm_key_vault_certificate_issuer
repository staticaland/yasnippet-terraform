# -*- mode: snippet -*-
# name: r_azurerm_key_vault_certificate_issuer
# key: r_azurerm_key_vault_certificate_issuer
# --

resource "azurerm_key_vault_certificate_issuer" "${1:this}" {
    key_vault_id = "${2:key_vault_id}"
    name = "${3:name}"
    org_id = "${4:org_id}"
    provider_name = "${5:provider_name}"
}