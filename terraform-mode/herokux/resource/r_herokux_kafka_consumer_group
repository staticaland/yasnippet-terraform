# -*- mode: snippet -*-
# name: r_herokux_kafka_consumer_group
# key: r_herokux_kafka_consumer_group
# --

resource "herokux_kafka_consumer_group" "${1:this}" {
    kafka_id = "${2:kafka_id}"
    name = "${3:name}"
}