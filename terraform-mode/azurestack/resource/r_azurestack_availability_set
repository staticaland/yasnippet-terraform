# -*- mode: snippet -*-
# name: r_azurestack_availability_set
# key: r_azurestack_availability_set
# --

resource "azurestack_availability_set" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
}