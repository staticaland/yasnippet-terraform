# -*- mode: snippet -*-
# name: r_azurestack_virtual_machine
# key: r_azurestack_virtual_machine
# --

resource "azurestack_virtual_machine" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    network_interface_ids = "${4:network_interface_ids}"
    resource_group_name = "${5:resource_group_name}"
    vm_size = "${6:vm_size}"
}