# -*- mode: snippet -*-
# name: d_azurestack_virtual_network
# key: d_azurestack_virtual_network
# --

data "azurestack_virtual_network" "${1:this}" {
    name = "${2:name}"
    resource_group_name = "${3:resource_group_name}"
}