# -*- mode: snippet -*-
# name: r_awx_credential_azure_key_vault
# key: r_awx_credential_azure_key_vault
# --

resource "awx_credential_azure_key_vault" "${1:this}" {
    client = "${2:client}"
    name = "${3:name}"
    organisation_id = "${4:organisation_id}"
    secret = "${5:secret}"
    tenant = "${6:tenant}"
    url = "${7:url}"
}