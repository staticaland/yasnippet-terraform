# -*- mode: snippet -*-
# name: r_awx_credential_scm
# key: r_awx_credential_scm
# --

resource "awx_credential_scm" "${1:this}" {
    name = "${2:name}"
    organisation_id = "${3:organisation_id}"
}