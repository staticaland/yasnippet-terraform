# -*- mode: snippet -*-
# name: r_awx_inventory_source
# key: r_awx_inventory_source
# --

resource "awx_inventory_source" "${1:this}" {
    inventory_id = "${2:inventory_id}"
    name = "${3:name}"
    source_project_id = "${4:source_project_id}"
}