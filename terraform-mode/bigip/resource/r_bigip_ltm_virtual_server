# -*- mode: snippet -*-
# name: r_bigip_ltm_virtual_server
# key: r_bigip_ltm_virtual_server
# --

resource "bigip_ltm_virtual_server" "${1:this}" {
    destination = "${2:destination}"
    name = "${3:name}"
    port = "${4:port}"
}