# -*- mode: snippet -*-
# name: r_mackerel_host_monitor
# key: r_mackerel_host_monitor
# --

resource "mackerel_host_monitor" "${1:this}" {
    critical = "${2:critical}"
    duration = "${3:duration}"
    metric = "${4:metric}"
    name = "${5:name}"
    operator = "${6:operator}"
    warning = "${7:warning}"
}