# -*- mode: snippet -*-
# name: d_aws_lambda_invocation
# key: d_aws_lambda_invocation
# --

data "aws_lambda_invocation" "${1:this}" {
    function_name = "${2:function_name}"
    input = "${3:input}"
}