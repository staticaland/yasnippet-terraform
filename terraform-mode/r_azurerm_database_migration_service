# -*- mode: snippet -*-
# name: r_azurerm_database_migration_service
# key: r_azurerm_database_migration_service
# --

resource "azurerm_database_migration_service" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    sku_name = "${5:sku_name}"
    subnet_id = "${6:subnet_id}"
}