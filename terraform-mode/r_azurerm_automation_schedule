# -*- mode: snippet -*-
# name: r_azurerm_automation_schedule
# key: r_azurerm_automation_schedule
# --

resource "azurerm_automation_schedule" "${1:this}" {
    automation_account_name = "${2:automation_account_name}"
    frequency = "${3:frequency}"
    name = "${4:name}"
    resource_group_name = "${5:resource_group_name}"
}