# -*- mode: snippet -*-
# name: r_huaweicloud_vbs_backup
# key: r_huaweicloud_vbs_backup
# --

resource "huaweicloud_vbs_backup" "${1:this}" {
    name = "${2:name}"
    volume_id = "${3:volume_id}"
}