# -*- mode: snippet -*-
# name: r_huaweicloud_dms_instance
# key: r_huaweicloud_dms_instance
# --

resource "huaweicloud_dms_instance" "${1:this}" {
    available_zones = "${2:available_zones}"
    engine = "${3:engine}"
    name = "${4:name}"
    product_id = "${5:product_id}"
    security_group_id = "${6:security_group_id}"
    storage_space = "${7:storage_space}"
    subnet_id = "${8:subnet_id}"
    vpc_id = "${9:vpc_id}"
}