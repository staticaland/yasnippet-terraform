# -*- mode: snippet -*-
# name: r_huaweicloud_gaussdb_opengauss_instance
# key: r_huaweicloud_gaussdb_opengauss_instance
# --

resource "huaweicloud_gaussdb_opengauss_instance" "${1:this}" {
    availability_zone = "${2:availability_zone}"
    flavor = "${3:flavor}"
    name = "${4:name}"
    password = "${5:password}"
    subnet_id = "${6:subnet_id}"
    vpc_id = "${7:vpc_id}"
}