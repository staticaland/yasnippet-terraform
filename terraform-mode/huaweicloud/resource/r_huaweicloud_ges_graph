# -*- mode: snippet -*-
# name: r_huaweicloud_ges_graph
# key: r_huaweicloud_ges_graph
# --

resource "huaweicloud_ges_graph" "${1:this}" {
    availability_zone = "${2:availability_zone}"
    graph_size_type = "${3:graph_size_type}"
    name = "${4:name}"
    region = "${5:region}"
    security_group_id = "${6:security_group_id}"
    subnet_id = "${7:subnet_id}"
    vpc_id = "${8:vpc_id}"
}