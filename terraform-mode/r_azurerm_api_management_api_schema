# -*- mode: snippet -*-
# name: r_azurerm_api_management_api_schema
# key: r_azurerm_api_management_api_schema
# --

resource "azurerm_api_management_api_schema" "${1:this}" {
    api_management_name = "${2:api_management_name}"
    api_name = "${3:api_name}"
    content_type = "${4:content_type}"
    resource_group_name = "${5:resource_group_name}"
    schema_id = "${6:schema_id}"
    value = "${7:value}"
}