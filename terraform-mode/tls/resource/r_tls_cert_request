# -*- mode: snippet -*-
# name: r_tls_cert_request
# key: r_tls_cert_request
# --

resource "tls_cert_request" "${1:this}" {
    key_algorithm = "${2:key_algorithm}"
    private_key_pem = "${3:private_key_pem}"
}