# -*- mode: snippet -*-
# name: d_aiven_service_user
# key: d_aiven_service_user
# --

data "aiven_service_user" "${1:this}" {
    project = "${2:project}"
    service_name = "${3:service_name}"
    username = "${4:username}"
}