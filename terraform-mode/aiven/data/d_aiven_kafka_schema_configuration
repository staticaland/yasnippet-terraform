# -*- mode: snippet -*-
# name: d_aiven_kafka_schema_configuration
# key: d_aiven_kafka_schema_configuration
# --

data "aiven_kafka_schema_configuration" "${1:this}" {
    project = "${2:project}"
    service_name = "${3:service_name}"
}