# -*- mode: snippet -*-
# name: d_aiven_kafka_schema
# key: d_aiven_kafka_schema
# --

data "aiven_kafka_schema" "${1:this}" {
    project = "${2:project}"
    service_name = "${3:service_name}"
    subject_name = "${4:subject_name}"
}