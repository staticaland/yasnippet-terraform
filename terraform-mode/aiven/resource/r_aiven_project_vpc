# -*- mode: snippet -*-
# name: r_aiven_project_vpc
# key: r_aiven_project_vpc
# --

resource "aiven_project_vpc" "${1:this}" {
    cloud_name = "${2:cloud_name}"
    network_cidr = "${3:network_cidr}"
    project = "${4:project}"
}