# -*- mode: snippet -*-
# name: r_azurerm_dev_test_policy
# key: r_azurerm_dev_test_policy
# --

resource "azurerm_dev_test_policy" "${1:this}" {
    evaluator_type = "${2:evaluator_type}"
    lab_name = "${3:lab_name}"
    name = "${4:name}"
    policy_set_name = "${5:policy_set_name}"
    resource_group_name = "${6:resource_group_name}"
    threshold = "${7:threshold}"
}