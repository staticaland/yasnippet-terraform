# -*- mode: snippet -*-
# name: r_packet_ip_attachment
# key: r_packet_ip_attachment
# --

resource "packet_ip_attachment" "${1:this}" {
    cidr_notation = "${2:cidr_notation}"
    device_id = "${3:device_id}"
}