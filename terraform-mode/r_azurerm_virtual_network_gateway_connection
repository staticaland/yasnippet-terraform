# -*- mode: snippet -*-
# name: r_azurerm_virtual_network_gateway_connection
# key: r_azurerm_virtual_network_gateway_connection
# --

resource "azurerm_virtual_network_gateway_connection" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    type = "${5:type}"
    virtual_network_gateway_id = "${6:virtual_network_gateway_id}"
}