# -*- mode: snippet -*-
# name: r_aws_emr_cluster
# key: r_aws_emr_cluster
# --

resource "aws_emr_cluster" "${1:this}" {
    name = "${2:name}"
    release_label = "${3:release_label}"
    service_role = "${4:service_role}"
}