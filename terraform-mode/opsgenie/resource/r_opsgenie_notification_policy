# -*- mode: snippet -*-
# name: r_opsgenie_notification_policy
# key: r_opsgenie_notification_policy
# --

resource "opsgenie_notification_policy" "${1:this}" {
    name = "${2:name}"
    team_id = "${3:team_id}"
}