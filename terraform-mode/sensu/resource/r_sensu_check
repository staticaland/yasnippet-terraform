# -*- mode: snippet -*-
# name: r_sensu_check
# key: r_sensu_check
# --

resource "sensu_check" "${1:this}" {
    command = "${2:command}"
    name = "${3:name}"
    subscriptions = "${4:subscriptions}"
}