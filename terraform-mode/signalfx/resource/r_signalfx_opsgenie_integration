# -*- mode: snippet -*-
# name: r_signalfx_opsgenie_integration
# key: r_signalfx_opsgenie_integration
# --

resource "signalfx_opsgenie_integration" "${1:this}" {
    api_key = "${2:api_key}"
    enabled = "${3:enabled}"
    name = "${4:name}"
}