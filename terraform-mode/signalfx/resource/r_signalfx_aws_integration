# -*- mode: snippet -*-
# name: r_signalfx_aws_integration
# key: r_signalfx_aws_integration
# --

resource "signalfx_aws_integration" "${1:this}" {
    enabled = "${2:enabled}"
    integration_id = "${3:integration_id}"
}