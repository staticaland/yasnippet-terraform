# -*- mode: snippet -*-
# name: r_keycloak_openid_client_service_account_realm_role
# key: r_keycloak_openid_client_service_account_realm_role
# --

resource "keycloak_openid_client_service_account_realm_role" "${1:this}" {
    realm_id = "${2:realm_id}"
    role = "${3:role}"
    service_account_user_id = "${4:service_account_user_id}"
}