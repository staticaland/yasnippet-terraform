# -*- mode: snippet -*-
# name: r_keycloak_authentication_flow
# key: r_keycloak_authentication_flow
# --

resource "keycloak_authentication_flow" "${1:this}" {
    alias = "${2:alias}"
    realm_id = "${3:realm_id}"
}