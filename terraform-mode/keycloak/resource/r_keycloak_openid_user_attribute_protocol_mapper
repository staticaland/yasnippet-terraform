# -*- mode: snippet -*-
# name: r_keycloak_openid_user_attribute_protocol_mapper
# key: r_keycloak_openid_user_attribute_protocol_mapper
# --

resource "keycloak_openid_user_attribute_protocol_mapper" "${1:this}" {
    claim_name = "${2:claim_name}"
    name = "${3:name}"
    realm_id = "${4:realm_id}"
    user_attribute = "${5:user_attribute}"
}