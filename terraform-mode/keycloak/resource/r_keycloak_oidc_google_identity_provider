# -*- mode: snippet -*-
# name: r_keycloak_oidc_google_identity_provider
# key: r_keycloak_oidc_google_identity_provider
# --

resource "keycloak_oidc_google_identity_provider" "${1:this}" {
    client_id = "${2:client_id}"
    client_secret = "${3:client_secret}"
    realm = "${4:realm}"
}