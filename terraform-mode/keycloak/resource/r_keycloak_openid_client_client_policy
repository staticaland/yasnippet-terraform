# -*- mode: snippet -*-
# name: r_keycloak_openid_client_client_policy
# key: r_keycloak_openid_client_client_policy
# --

resource "keycloak_openid_client_client_policy" "${1:this}" {
    clients = "${2:clients}"
    name = "${3:name}"
    realm_id = "${4:realm_id}"
    resource_server_id = "${5:resource_server_id}"
}