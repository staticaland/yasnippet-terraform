# -*- mode: snippet -*-
# name: r_keycloak_openid_client_authorization_permission
# key: r_keycloak_openid_client_authorization_permission
# --

resource "keycloak_openid_client_authorization_permission" "${1:this}" {
    name = "${2:name}"
    realm_id = "${3:realm_id}"
    resource_server_id = "${4:resource_server_id}"
}