# -*- mode: snippet -*-
# name: r_keycloak_openid_group_membership_protocol_mapper
# key: r_keycloak_openid_group_membership_protocol_mapper
# --

resource "keycloak_openid_group_membership_protocol_mapper" "${1:this}" {
    claim_name = "${2:claim_name}"
    name = "${3:name}"
    realm_id = "${4:realm_id}"
}