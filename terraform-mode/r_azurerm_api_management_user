# -*- mode: snippet -*-
# name: r_azurerm_api_management_user
# key: r_azurerm_api_management_user
# --

resource "azurerm_api_management_user" "${1:this}" {
    api_management_name = "${2:api_management_name}"
    email = "${3:email}"
    first_name = "${4:first_name}"
    last_name = "${5:last_name}"
    resource_group_name = "${6:resource_group_name}"
    user_id = "${7:user_id}"
}