# -*- mode: snippet -*-
# name: r_aws_codebuild_source_credential
# key: r_aws_codebuild_source_credential
# --

resource "aws_codebuild_source_credential" "${1:this}" {
    auth_type = "${2:auth_type}"
    server_type = "${3:server_type}"
    token = "${4:token}"
}