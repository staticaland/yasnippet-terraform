# -*- mode: snippet -*-
# name: r_checkpoint_management_service_sctp
# key: r_checkpoint_management_service_sctp
# --

resource "checkpoint_management_service_sctp" "${1:this}" {
    name = "${2:name}"
}