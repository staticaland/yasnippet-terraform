# -*- mode: snippet -*-
# name: r_digitalocean_kubernetes_cluster
# key: r_digitalocean_kubernetes_cluster
# --

resource "digitalocean_kubernetes_cluster" "${1:this}" {
    name = "${2:name}"
    region = "${3:region}"
    version = "${4:version}"
}