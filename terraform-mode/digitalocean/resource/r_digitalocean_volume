# -*- mode: snippet -*-
# name: r_digitalocean_volume
# key: r_digitalocean_volume
# --

resource "digitalocean_volume" "${1:this}" {
    name = "${2:name}"
    region = "${3:region}"
    size = "${4:size}"
}