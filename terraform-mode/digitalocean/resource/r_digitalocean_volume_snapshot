# -*- mode: snippet -*-
# name: r_digitalocean_volume_snapshot
# key: r_digitalocean_volume_snapshot
# --

resource "digitalocean_volume_snapshot" "${1:this}" {
    name = "${2:name}"
    volume_id = "${3:volume_id}"
}