# -*- mode: snippet -*-
# name: r_digitalocean_loadbalancer
# key: r_digitalocean_loadbalancer
# --

resource "digitalocean_loadbalancer" "${1:this}" {
    name = "${2:name}"
    region = "${3:region}"
}