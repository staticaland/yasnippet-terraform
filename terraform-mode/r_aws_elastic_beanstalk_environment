# -*- mode: snippet -*-
# name: r_aws_elastic_beanstalk_environment
# key: r_aws_elastic_beanstalk_environment
# --

resource "aws_elastic_beanstalk_environment" "${1:this}" {
    application = "${2:application}"
    name = "${3:name}"
}