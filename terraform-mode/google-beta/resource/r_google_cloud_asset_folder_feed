# -*- mode: snippet -*-
# name: r_google_cloud_asset_folder_feed
# key: r_google_cloud_asset_folder_feed
# --

resource "google_cloud_asset_folder_feed" "${1:this}" {
    billing_project = "${2:billing_project}"
    feed_id = "${3:feed_id}"
    folder = "${4:folder}"
}