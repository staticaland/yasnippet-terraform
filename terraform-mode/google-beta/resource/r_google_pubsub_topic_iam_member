# -*- mode: snippet -*-
# name: r_google_pubsub_topic_iam_member
# key: r_google_pubsub_topic_iam_member
# --

resource "google_pubsub_topic_iam_member" "${1:this}" {
    member = "${2:member}"
    role = "${3:role}"
    topic = "${4:topic}"
}