# -*- mode: snippet -*-
# name: r_google_spanner_database_iam_binding
# key: r_google_spanner_database_iam_binding
# --

resource "google_spanner_database_iam_binding" "${1:this}" {
    database = "${2:database}"
    instance = "${3:instance}"
    members = "${4:members}"
    role = "${5:role}"
}