# -*- mode: snippet -*-
# name: r_outscale_volumes_link
# key: r_outscale_volumes_link
# --

resource "outscale_volumes_link" "${1:this}" {
    device_name = "${2:device_name}"
    vm_id = "${3:vm_id}"
    volume_id = "${4:volume_id}"
}