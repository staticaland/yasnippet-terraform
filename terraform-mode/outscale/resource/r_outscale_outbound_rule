# -*- mode: snippet -*-
# name: r_outscale_outbound_rule
# key: r_outscale_outbound_rule
# --

resource "outscale_outbound_rule" "${1:this}" {
    flow = "${2:flow}"
    security_group_id = "${3:security_group_id}"
}