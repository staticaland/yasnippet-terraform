# -*- mode: snippet -*-
# name: d_azurerm_network_watcher
# key: d_azurerm_network_watcher
# --

data "azurerm_network_watcher" "${1:this}" {
    name = "${2:name}"
    resource_group_name = "${3:resource_group_name}"
}