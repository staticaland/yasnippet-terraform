# -*- mode: snippet -*-
# name: r_aptible_endpoint
# key: r_aptible_endpoint
# --

resource "aptible_endpoint" "${1:this}" {
    env_id = "${2:env_id}"
    resource_id = "${3:resource_id}"
    resource_type = "${4:resource_type}"
}