# -*- mode: snippet -*-
# name: r_aws_app_cookie_stickiness_policy
# key: r_aws_app_cookie_stickiness_policy
# --

resource "aws_app_cookie_stickiness_policy" "${1:this}" {
    cookie_name = "${2:cookie_name}"
    lb_port = "${3:lb_port}"
    load_balancer = "${4:load_balancer}"
    name = "${5:name}"
}