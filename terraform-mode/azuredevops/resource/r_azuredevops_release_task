# -*- mode: snippet -*-
# name: r_azuredevops_release_task
# key: r_azuredevops_release_task
# --

resource "azuredevops_release_task" "${1:this}" {
    definition_id = "${2:definition_id}"
    project_id = "${3:project_id}"
    stage_name = "${4:stage_name}"
}