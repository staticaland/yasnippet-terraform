# -*- mode: snippet -*-
# name: r_azuredevops_release_tasks
# key: r_azuredevops_release_tasks
# --

resource "azuredevops_release_tasks" "${1:this}" {
    definition_id = "${2:definition_id}"
    project_id = "${3:project_id}"
}