# -*- mode: snippet -*-
# name: r_azurerm_log_analytics_datasource_windows_performance_counter
# key: r_azurerm_log_analytics_datasource_windows_performance_counter
# --

resource "azurerm_log_analytics_datasource_windows_performance_counter" "${1:this}" {
    counter_name = "${2:counter_name}"
    instance_name = "${3:instance_name}"
    interval_seconds = "${4:interval_seconds}"
    name = "${5:name}"
    object_name = "${6:object_name}"
    resource_group_name = "${7:resource_group_name}"
    workspace_name = "${8:workspace_name}"
}