# -*- mode: snippet -*-
# name: r_aws_wafv2_ip_set
# key: r_aws_wafv2_ip_set
# --

resource "aws_wafv2_ip_set" "${1:this}" {
    ip_address_version = "${2:ip_address_version}"
    name = "${3:name}"
    scope = "${4:scope}"
}