# -*- mode: snippet -*-
# name: r_tencentcloud_elasticsearch_instance
# key: r_tencentcloud_elasticsearch_instance
# --

resource "tencentcloud_elasticsearch_instance" "${1:this}" {
    availability_zone = "${2:availability_zone}"
    password = "${3:password}"
    subnet_id = "${4:subnet_id}"
    version = "${5:version}"
    vpc_id = "${6:vpc_id}"
}