# -*- mode: snippet -*-
# name: r_tencentcloud_clb_listener
# key: r_tencentcloud_clb_listener
# --

resource "tencentcloud_clb_listener" "${1:this}" {
    clb_id = "${2:clb_id}"
    listener_name = "${3:listener_name}"
    protocol = "${4:protocol}"
}