# -*- mode: snippet -*-
# name: r_tencentcloud_sqlserver_account
# key: r_tencentcloud_sqlserver_account
# --

resource "tencentcloud_sqlserver_account" "${1:this}" {
    instance_id = "${2:instance_id}"
    name = "${3:name}"
    password = "${4:password}"
}