# -*- mode: snippet -*-
# name: r_google_dns_record_set
# key: r_google_dns_record_set
# --

resource "google_dns_record_set" "${1:this}" {
    managed_zone = "${2:managed_zone}"
    name = "${3:name}"
    rrdatas = "${4:rrdatas}"
    ttl = "${5:ttl}"
    type = "${6:type}"
}