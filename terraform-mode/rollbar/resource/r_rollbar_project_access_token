# -*- mode: snippet -*-
# name: r_rollbar_project_access_token
# key: r_rollbar_project_access_token
# --

resource "rollbar_project_access_token" "${1:this}" {
    name = "${2:name}"
    project_id = "${3:project_id}"
    scopes = "${4:scopes}"
    status = "${5:status}"
}