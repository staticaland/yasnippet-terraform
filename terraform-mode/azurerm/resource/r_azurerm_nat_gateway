# -*- mode: snippet -*-
# name: r_azurerm_nat_gateway
# key: r_azurerm_nat_gateway
# --

resource "azurerm_nat_gateway" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
}