# -*- mode: snippet -*-
# name: r_azurerm_postgresql_configuration
# key: r_azurerm_postgresql_configuration
# --

resource "azurerm_postgresql_configuration" "${1:this}" {
    name = "${2:name}"
    resource_group_name = "${3:resource_group_name}"
    server_name = "${4:server_name}"
    value = "${5:value}"
}