# -*- mode: snippet -*-
# name: r_azurerm_relay_namespace
# key: r_azurerm_relay_namespace
# --

resource "azurerm_relay_namespace" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    sku_name = "${5:sku_name}"
}