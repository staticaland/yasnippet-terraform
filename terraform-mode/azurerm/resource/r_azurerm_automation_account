# -*- mode: snippet -*-
# name: r_azurerm_automation_account
# key: r_azurerm_automation_account
# --

resource "azurerm_automation_account" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    sku_name = "${5:sku_name}"
}