# -*- mode: snippet -*-
# name: r_azurerm_hdinsight_spark_cluster
# key: r_azurerm_hdinsight_spark_cluster
# --

resource "azurerm_hdinsight_spark_cluster" "${1:this}" {
    cluster_version = "${2:cluster_version}"
    location = "${3:location}"
    name = "${4:name}"
    resource_group_name = "${5:resource_group_name}"
    tier = "${6:tier}"
}