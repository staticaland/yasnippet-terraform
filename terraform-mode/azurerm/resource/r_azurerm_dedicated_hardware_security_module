# -*- mode: snippet -*-
# name: r_azurerm_dedicated_hardware_security_module
# key: r_azurerm_dedicated_hardware_security_module
# --

resource "azurerm_dedicated_hardware_security_module" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    sku_name = "${5:sku_name}"
    stamp_id = "${6:stamp_id}"
}