# -*- mode: snippet -*-
# name: r_azurerm_route
# key: r_azurerm_route
# --

resource "azurerm_route" "${1:this}" {
    address_prefix = "${2:address_prefix}"
    name = "${3:name}"
    next_hop_type = "${4:next_hop_type}"
    resource_group_name = "${5:resource_group_name}"
    route_table_name = "${6:route_table_name}"
}