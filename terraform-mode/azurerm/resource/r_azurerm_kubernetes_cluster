# -*- mode: snippet -*-
# name: r_azurerm_kubernetes_cluster
# key: r_azurerm_kubernetes_cluster
# --

resource "azurerm_kubernetes_cluster" "${1:this}" {
    dns_prefix = "${2:dns_prefix}"
    location = "${3:location}"
    name = "${4:name}"
    resource_group_name = "${5:resource_group_name}"
}