# -*- mode: snippet -*-
# name: r_azurerm_app_service_hybrid_connection
# key: r_azurerm_app_service_hybrid_connection
# --

resource "azurerm_app_service_hybrid_connection" "${1:this}" {
    app_service_name = "${2:app_service_name}"
    hostname = "${3:hostname}"
    port = "${4:port}"
    relay_id = "${5:relay_id}"
    resource_group_name = "${6:resource_group_name}"
}