# -*- mode: snippet -*-
# name: r_azurerm_automation_credential
# key: r_azurerm_automation_credential
# --

resource "azurerm_automation_credential" "${1:this}" {
    automation_account_name = "${2:automation_account_name}"
    name = "${3:name}"
    password = "${4:password}"
    resource_group_name = "${5:resource_group_name}"
    username = "${6:username}"
}