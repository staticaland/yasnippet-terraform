# -*- mode: snippet -*-
# name: r_azurerm_windows_virtual_machine
# key: r_azurerm_windows_virtual_machine
# --

resource "azurerm_windows_virtual_machine" "${1:this}" {
    admin_password = "${2:admin_password}"
    admin_username = "${3:admin_username}"
    location = "${4:location}"
    name = "${5:name}"
    network_interface_ids = "${6:network_interface_ids}"
    resource_group_name = "${7:resource_group_name}"
    size = "${8:size}"
}