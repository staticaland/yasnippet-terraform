# -*- mode: snippet -*-
# name: r_azurerm_api_management_identity_provider_facebook
# key: r_azurerm_api_management_identity_provider_facebook
# --

resource "azurerm_api_management_identity_provider_facebook" "${1:this}" {
    api_management_name = "${2:api_management_name}"
    app_id = "${3:app_id}"
    app_secret = "${4:app_secret}"
    resource_group_name = "${5:resource_group_name}"
}