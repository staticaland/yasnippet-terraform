# -*- mode: snippet -*-
# name: r_azurerm_mssql_elasticpool
# key: r_azurerm_mssql_elasticpool
# --

resource "azurerm_mssql_elasticpool" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    server_name = "${5:server_name}"
}