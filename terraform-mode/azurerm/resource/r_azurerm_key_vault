# -*- mode: snippet -*-
# name: r_azurerm_key_vault
# key: r_azurerm_key_vault
# --

resource "azurerm_key_vault" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    sku_name = "${5:sku_name}"
    tenant_id = "${6:tenant_id}"
}