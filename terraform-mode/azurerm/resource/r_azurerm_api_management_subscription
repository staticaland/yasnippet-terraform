# -*- mode: snippet -*-
# name: r_azurerm_api_management_subscription
# key: r_azurerm_api_management_subscription
# --

resource "azurerm_api_management_subscription" "${1:this}" {
    api_management_name = "${2:api_management_name}"
    display_name = "${3:display_name}"
    resource_group_name = "${4:resource_group_name}"
    user_id = "${5:user_id}"
}