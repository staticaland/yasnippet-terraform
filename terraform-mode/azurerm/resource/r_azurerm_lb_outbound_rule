# -*- mode: snippet -*-
# name: r_azurerm_lb_outbound_rule
# key: r_azurerm_lb_outbound_rule
# --

resource "azurerm_lb_outbound_rule" "${1:this}" {
    backend_address_pool_id = "${2:backend_address_pool_id}"
    loadbalancer_id = "${3:loadbalancer_id}"
    name = "${4:name}"
    protocol = "${5:protocol}"
    resource_group_name = "${6:resource_group_name}"
}