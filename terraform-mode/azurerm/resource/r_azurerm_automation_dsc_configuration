# -*- mode: snippet -*-
# name: r_azurerm_automation_dsc_configuration
# key: r_azurerm_automation_dsc_configuration
# --

resource "azurerm_automation_dsc_configuration" "${1:this}" {
    automation_account_name = "${2:automation_account_name}"
    content_embedded = "${3:content_embedded}"
    location = "${4:location}"
    name = "${5:name}"
    resource_group_name = "${6:resource_group_name}"
}