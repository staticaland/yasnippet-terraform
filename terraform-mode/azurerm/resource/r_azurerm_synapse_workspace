# -*- mode: snippet -*-
# name: r_azurerm_synapse_workspace
# key: r_azurerm_synapse_workspace
# --

resource "azurerm_synapse_workspace" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    sql_administrator_login = "${5:sql_administrator_login}"
    sql_administrator_login_password = "${6:sql_administrator_login_password}"
    storage_data_lake_gen2_filesystem_id = "${7:storage_data_lake_gen2_filesystem_id}"
}