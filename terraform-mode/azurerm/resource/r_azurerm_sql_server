# -*- mode: snippet -*-
# name: r_azurerm_sql_server
# key: r_azurerm_sql_server
# --

resource "azurerm_sql_server" "${1:this}" {
    administrator_login = "${2:administrator_login}"
    administrator_login_password = "${3:administrator_login_password}"
    location = "${4:location}"
    name = "${5:name}"
    resource_group_name = "${6:resource_group_name}"
    version = "${7:version}"
}