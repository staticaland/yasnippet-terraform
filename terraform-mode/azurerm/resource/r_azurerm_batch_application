# -*- mode: snippet -*-
# name: r_azurerm_batch_application
# key: r_azurerm_batch_application
# --

resource "azurerm_batch_application" "${1:this}" {
    account_name = "${2:account_name}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
}