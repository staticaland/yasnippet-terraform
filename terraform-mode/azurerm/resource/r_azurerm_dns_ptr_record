# -*- mode: snippet -*-
# name: r_azurerm_dns_ptr_record
# key: r_azurerm_dns_ptr_record
# --

resource "azurerm_dns_ptr_record" "${1:this}" {
    name = "${2:name}"
    records = "${3:records}"
    resource_group_name = "${4:resource_group_name}"
    ttl = "${5:ttl}"
    zone_name = "${6:zone_name}"
}