# -*- mode: snippet -*-
# name: r_azurerm_api_management_api
# key: r_azurerm_api_management_api
# --

resource "azurerm_api_management_api" "${1:this}" {
    api_management_name = "${2:api_management_name}"
    display_name = "${3:display_name}"
    name = "${4:name}"
    path = "${5:path}"
    protocols = "${6:protocols}"
    resource_group_name = "${7:resource_group_name}"
    revision = "${8:revision}"
}