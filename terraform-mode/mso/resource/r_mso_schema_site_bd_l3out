# -*- mode: snippet -*-
# name: r_mso_schema_site_bd_l3out
# key: r_mso_schema_site_bd_l3out
# --

resource "mso_schema_site_bd_l3out" "${1:this}" {
    bd_name = "${2:bd_name}"
    l3out_name = "${3:l3out_name}"
    schema_id = "${4:schema_id}"
    site_id = "${5:site_id}"
    template_name = "${6:template_name}"
}