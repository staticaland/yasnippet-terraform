# -*- mode: snippet -*-
# name: r_mso_schema_site_external_epg_selector
# key: r_mso_schema_site_external_epg_selector
# --

resource "mso_schema_site_external_epg_selector" "${1:this}" {
    external_epg_name = "${2:external_epg_name}"
    ip = "${3:ip}"
    name = "${4:name}"
    schema_id = "${5:schema_id}"
    site_id = "${6:site_id}"
    template_name = "${7:template_name}"
}