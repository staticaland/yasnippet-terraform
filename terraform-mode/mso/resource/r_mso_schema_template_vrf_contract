# -*- mode: snippet -*-
# name: r_mso_schema_template_vrf_contract
# key: r_mso_schema_template_vrf_contract
# --

resource "mso_schema_template_vrf_contract" "${1:this}" {
    contract_name = "${2:contract_name}"
    relationship_type = "${3:relationship_type}"
    schema_id = "${4:schema_id}"
    template_name = "${5:template_name}"
    vrf_name = "${6:vrf_name}"
}