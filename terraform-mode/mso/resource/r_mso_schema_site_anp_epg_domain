# -*- mode: snippet -*-
# name: r_mso_schema_site_anp_epg_domain
# key: r_mso_schema_site_anp_epg_domain
# --

resource "mso_schema_site_anp_epg_domain" "${1:this}" {
    anp_name = "${2:anp_name}"
    deploy_immediacy = "${3:deploy_immediacy}"
    dn = "${4:dn}"
    domain_type = "${5:domain_type}"
    epg_name = "${6:epg_name}"
    resolution_immediacy = "${7:resolution_immediacy}"
    schema_id = "${8:schema_id}"
    site_id = "${9:site_id}"
    template_name = "${10:template_name}"
}