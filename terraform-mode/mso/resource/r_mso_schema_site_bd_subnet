# -*- mode: snippet -*-
# name: r_mso_schema_site_bd_subnet
# key: r_mso_schema_site_bd_subnet
# --

resource "mso_schema_site_bd_subnet" "${1:this}" {
    bd_name = "${2:bd_name}"
    ip = "${3:ip}"
    schema_id = "${4:schema_id}"
    site_id = "${5:site_id}"
    template_name = "${6:template_name}"
}