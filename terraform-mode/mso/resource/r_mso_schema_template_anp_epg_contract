# -*- mode: snippet -*-
# name: r_mso_schema_template_anp_epg_contract
# key: r_mso_schema_template_anp_epg_contract
# --

resource "mso_schema_template_anp_epg_contract" "${1:this}" {
    anp_name = "${2:anp_name}"
    contract_name = "${3:contract_name}"
    epg_name = "${4:epg_name}"
    relationship_type = "${5:relationship_type}"
    schema_id = "${6:schema_id}"
    template_name = "${7:template_name}"
}