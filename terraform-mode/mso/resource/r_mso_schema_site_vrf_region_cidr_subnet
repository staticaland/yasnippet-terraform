# -*- mode: snippet -*-
# name: r_mso_schema_site_vrf_region_cidr_subnet
# key: r_mso_schema_site_vrf_region_cidr_subnet
# --

resource "mso_schema_site_vrf_region_cidr_subnet" "${1:this}" {
    cidr_ip = "${2:cidr_ip}"
    ip = "${3:ip}"
    region_name = "${4:region_name}"
    schema_id = "${5:schema_id}"
    site_id = "${6:site_id}"
    template_name = "${7:template_name}"
    vrf_name = "${8:vrf_name}"
    zone = "${9:zone}"
}