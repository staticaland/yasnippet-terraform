# -*- mode: snippet -*-
# name: r_mso_schema_site_anp
# key: r_mso_schema_site_anp
# --

resource "mso_schema_site_anp" "${1:this}" {
    anp_name = "${2:anp_name}"
    schema_id = "${3:schema_id}"
    site_id = "${4:site_id}"
    template_name = "${5:template_name}"
}