# -*- mode: snippet -*-
# name: r_mso_schema_template_l3out
# key: r_mso_schema_template_l3out
# --

resource "mso_schema_template_l3out" "${1:this}" {
    display_name = "${2:display_name}"
    l3out_name = "${3:l3out_name}"
    schema_id = "${4:schema_id}"
    template_name = "${5:template_name}"
    vrf_name = "${6:vrf_name}"
}