# -*- mode: snippet -*-
# name: r_mso_schema_site_vrf
# key: r_mso_schema_site_vrf
# --

resource "mso_schema_site_vrf" "${1:this}" {
    schema_id = "${2:schema_id}"
    site_id = "${3:site_id}"
    template_name = "${4:template_name}"
    vrf_name = "${5:vrf_name}"
}