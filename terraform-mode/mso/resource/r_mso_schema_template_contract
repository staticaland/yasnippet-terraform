# -*- mode: snippet -*-
# name: r_mso_schema_template_contract
# key: r_mso_schema_template_contract
# --

resource "mso_schema_template_contract" "${1:this}" {
    contract_name = "${2:contract_name}"
    directives = "${3:directives}"
    filter_relationships = "${4:filter_relationships}"
    schema_id = "${5:schema_id}"
    template_name = "${6:template_name}"
}