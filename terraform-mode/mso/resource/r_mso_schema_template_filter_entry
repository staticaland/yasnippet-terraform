# -*- mode: snippet -*-
# name: r_mso_schema_template_filter_entry
# key: r_mso_schema_template_filter_entry
# --

resource "mso_schema_template_filter_entry" "${1:this}" {
    display_name = "${2:display_name}"
    entry_display_name = "${3:entry_display_name}"
    entry_name = "${4:entry_name}"
    name = "${5:name}"
    schema_id = "${6:schema_id}"
    template_name = "${7:template_name}"
}