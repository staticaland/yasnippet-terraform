# -*- mode: snippet -*-
# name: r_mso_schema_template
# key: r_mso_schema_template
# --

resource "mso_schema_template" "${1:this}" {
    display_name = "${2:display_name}"
    name = "${3:name}"
    schema_id = "${4:schema_id}"
    tenant_id = "${5:tenant_id}"
}