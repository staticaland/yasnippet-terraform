# -*- mode: snippet -*-
# name: r_mso_schema_template_vrf
# key: r_mso_schema_template_vrf
# --

resource "mso_schema_template_vrf" "${1:this}" {
    display_name = "${2:display_name}"
    name = "${3:name}"
    schema_id = "${4:schema_id}"
    template = "${5:template}"
}