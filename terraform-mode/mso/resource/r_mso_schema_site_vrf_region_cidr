# -*- mode: snippet -*-
# name: r_mso_schema_site_vrf_region_cidr
# key: r_mso_schema_site_vrf_region_cidr
# --

resource "mso_schema_site_vrf_region_cidr" "${1:this}" {
    ip = "${2:ip}"
    primary = "${3:primary}"
    region_name = "${4:region_name}"
    schema_id = "${5:schema_id}"
    site_id = "${6:site_id}"
    template_name = "${7:template_name}"
    vrf_name = "${8:vrf_name}"
}