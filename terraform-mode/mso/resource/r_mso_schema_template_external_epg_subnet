# -*- mode: snippet -*-
# name: r_mso_schema_template_external_epg_subnet
# key: r_mso_schema_template_external_epg_subnet
# --

resource "mso_schema_template_external_epg_subnet" "${1:this}" {
    external_epg_name = "${2:external_epg_name}"
    ip = "${3:ip}"
    schema_id = "${4:schema_id}"
    template_name = "${5:template_name}"
}