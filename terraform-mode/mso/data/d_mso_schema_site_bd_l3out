# -*- mode: snippet -*-
# name: d_mso_schema_site_bd_l3out
# key: d_mso_schema_site_bd_l3out
# --

data "mso_schema_site_bd_l3out" "${1:this}" {
    bd_name = "${2:bd_name}"
    l3out_name = "${3:l3out_name}"
    schema_id = "${4:schema_id}"
    site_id = "${5:site_id}"
}