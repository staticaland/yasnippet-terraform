# -*- mode: snippet -*-
# name: d_mso_schema_site_vrf_region_cidr
# key: d_mso_schema_site_vrf_region_cidr
# --

data "mso_schema_site_vrf_region_cidr" "${1:this}" {
    ip = "${2:ip}"
    region_name = "${3:region_name}"
    schema_id = "${4:schema_id}"
    site_id = "${5:site_id}"
    vrf_name = "${6:vrf_name}"
}