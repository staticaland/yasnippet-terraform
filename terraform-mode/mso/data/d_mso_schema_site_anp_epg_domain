# -*- mode: snippet -*-
# name: d_mso_schema_site_anp_epg_domain
# key: d_mso_schema_site_anp_epg_domain
# --

data "mso_schema_site_anp_epg_domain" "${1:this}" {
    anp_name = "${2:anp_name}"
    dn = "${3:dn}"
    domain_type = "${4:domain_type}"
    epg_name = "${5:epg_name}"
    schema_id = "${6:schema_id}"
    site_id = "${7:site_id}"
}