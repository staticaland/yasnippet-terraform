# -*- mode: snippet -*-
# name: d_mso_schema_site_external_epg_selector
# key: d_mso_schema_site_external_epg_selector
# --

data "mso_schema_site_external_epg_selector" "${1:this}" {
    external_epg_name = "${2:external_epg_name}"
    name = "${3:name}"
    schema_id = "${4:schema_id}"
    site_id = "${5:site_id}"
    template_name = "${6:template_name}"
}