# -*- mode: snippet -*-
# name: d_mso_schema_template_external_epg_subnet
# key: d_mso_schema_template_external_epg_subnet
# --

data "mso_schema_template_external_epg_subnet" "${1:this}" {
    external_epg_name = "${2:external_epg_name}"
    ip = "${3:ip}"
    schema_id = "${4:schema_id}"
    template_name = "${5:template_name}"
}