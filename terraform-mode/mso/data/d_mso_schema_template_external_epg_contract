# -*- mode: snippet -*-
# name: d_mso_schema_template_external_epg_contract
# key: d_mso_schema_template_external_epg_contract
# --

data "mso_schema_template_external_epg_contract" "${1:this}" {
    contract_name = "${2:contract_name}"
    external_epg_name = "${3:external_epg_name}"
    schema_id = "${4:schema_id}"
    template_name = "${5:template_name}"
}