# -*- mode: snippet -*-
# name: d_mso_schema_site_vrf
# key: d_mso_schema_site_vrf
# --

data "mso_schema_site_vrf" "${1:this}" {
    schema_id = "${2:schema_id}"
    site_id = "${3:site_id}"
    vrf_name = "${4:vrf_name}"
}