# -*- mode: snippet -*-
# name: r_dme_dns_record
# key: r_dme_dns_record
# --

resource "dme_dns_record" "${1:this}" {
    domain_id = "${2:domain_id}"
    name = "${3:name}"
    ttl = "${4:ttl}"
    type = "${5:type}"
    value = "${6:value}"
}