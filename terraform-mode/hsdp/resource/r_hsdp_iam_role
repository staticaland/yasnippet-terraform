# -*- mode: snippet -*-
# name: r_hsdp_iam_role
# key: r_hsdp_iam_role
# --

resource "hsdp_iam_role" "${1:this}" {
    description = "${2:description}"
    managing_organization = "${3:managing_organization}"
    name = "${4:name}"
    permissions = "${5:permissions}"
}