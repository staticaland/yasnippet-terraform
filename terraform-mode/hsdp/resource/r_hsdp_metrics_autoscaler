# -*- mode: snippet -*-
# name: r_hsdp_metrics_autoscaler
# key: r_hsdp_metrics_autoscaler
# --

resource "hsdp_metrics_autoscaler" "${1:this}" {
    app_name = "${2:app_name}"
    metrics_instance_id = "${3:metrics_instance_id}"
}