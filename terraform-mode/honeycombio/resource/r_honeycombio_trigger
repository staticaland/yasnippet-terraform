# -*- mode: snippet -*-
# name: r_honeycombio_trigger
# key: r_honeycombio_trigger
# --

resource "honeycombio_trigger" "${1:this}" {
    dataset = "${2:dataset}"
    name = "${3:name}"
    query_json = "${4:query_json}"
}