# -*- mode: snippet -*-
# name: r_secrethub_service_aws
# key: r_secrethub_service_aws
# --

resource "secrethub_service_aws" "${1:this}" {
    kms_key_arn = "${2:kms_key_arn}"
    repo = "${3:repo}"
    role = "${4:role}"
}