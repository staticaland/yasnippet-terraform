# -*- mode: snippet -*-
# name: r_azurerm_private_dns_cname_record
# key: r_azurerm_private_dns_cname_record
# --

resource "azurerm_private_dns_cname_record" "${1:this}" {
    name = "${2:name}"
    record = "${3:record}"
    resource_group_name = "${4:resource_group_name}"
    ttl = "${5:ttl}"
    zone_name = "${6:zone_name}"
}