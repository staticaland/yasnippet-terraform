# -*- mode: snippet -*-
# name: r_oci_identity_smtp_credential
# key: r_oci_identity_smtp_credential
# --

resource "oci_identity_smtp_credential" "${1:this}" {
    description = "${2:description}"
    user_id = "${3:user_id}"
}