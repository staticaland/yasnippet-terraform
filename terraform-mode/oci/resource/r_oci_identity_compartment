# -*- mode: snippet -*-
# name: r_oci_identity_compartment
# key: r_oci_identity_compartment
# --

resource "oci_identity_compartment" "${1:this}" {
    description = "${2:description}"
    name = "${3:name}"
}