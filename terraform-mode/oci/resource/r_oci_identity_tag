# -*- mode: snippet -*-
# name: r_oci_identity_tag
# key: r_oci_identity_tag
# --

resource "oci_identity_tag" "${1:this}" {
    description = "${2:description}"
    name = "${3:name}"
    tag_namespace_id = "${4:tag_namespace_id}"
}