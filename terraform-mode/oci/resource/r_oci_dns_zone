# -*- mode: snippet -*-
# name: r_oci_dns_zone
# key: r_oci_dns_zone
# --

resource "oci_dns_zone" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    name = "${3:name}"
    zone_type = "${4:zone_type}"
}