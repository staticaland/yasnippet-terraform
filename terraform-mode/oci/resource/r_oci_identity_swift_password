# -*- mode: snippet -*-
# name: r_oci_identity_swift_password
# key: r_oci_identity_swift_password
# --

resource "oci_identity_swift_password" "${1:this}" {
    description = "${2:description}"
    user_id = "${3:user_id}"
}