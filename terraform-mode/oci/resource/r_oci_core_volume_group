# -*- mode: snippet -*-
# name: r_oci_core_volume_group
# key: r_oci_core_volume_group
# --

resource "oci_core_volume_group" "${1:this}" {
    availability_domain = "${2:availability_domain}"
    compartment_id = "${3:compartment_id}"
}