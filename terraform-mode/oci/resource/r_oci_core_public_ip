# -*- mode: snippet -*-
# name: r_oci_core_public_ip
# key: r_oci_core_public_ip
# --

resource "oci_core_public_ip" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    lifetime = "${3:lifetime}"
}