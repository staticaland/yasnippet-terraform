# -*- mode: snippet -*-
# name: r_oci_identity_auth_token
# key: r_oci_identity_auth_token
# --

resource "oci_identity_auth_token" "${1:this}" {
    description = "${2:description}"
    user_id = "${3:user_id}"
}