# -*- mode: snippet -*-
# name: r_oci_database_backup
# key: r_oci_database_backup
# --

resource "oci_database_backup" "${1:this}" {
    database_id = "${2:database_id}"
    display_name = "${3:display_name}"
}