# -*- mode: snippet -*-
# name: r_oci_datascience_notebook_session
# key: r_oci_datascience_notebook_session
# --

resource "oci_datascience_notebook_session" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    project_id = "${3:project_id}"
}