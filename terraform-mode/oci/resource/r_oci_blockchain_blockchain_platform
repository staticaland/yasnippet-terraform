# -*- mode: snippet -*-
# name: r_oci_blockchain_blockchain_platform
# key: r_oci_blockchain_blockchain_platform
# --

resource "oci_blockchain_blockchain_platform" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    compute_shape = "${3:compute_shape}"
    display_name = "${4:display_name}"
    platform_role = "${5:platform_role}"
}