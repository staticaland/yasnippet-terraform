# -*- mode: snippet -*-
# name: d_oci_limits_limit_definitions
# key: d_oci_limits_limit_definitions
# --

data "oci_limits_limit_definitions" "${1:this}" {
    compartment_id = "${2:compartment_id}"
}