# -*- mode: snippet -*-
# name: d_oci_core_boot_volumes
# key: d_oci_core_boot_volumes
# --

data "oci_core_boot_volumes" "${1:this}" {
    availability_domain = "${2:availability_domain}"
    compartment_id = "${3:compartment_id}"
}