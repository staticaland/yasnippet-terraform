# -*- mode: snippet -*-
# name: d_oci_kms_keys
# key: d_oci_kms_keys
# --

data "oci_kms_keys" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    management_endpoint = "${3:management_endpoint}"
}