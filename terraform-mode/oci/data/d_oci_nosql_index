# -*- mode: snippet -*-
# name: d_oci_nosql_index
# key: d_oci_nosql_index
# --

data "oci_nosql_index" "${1:this}" {
    compartment_id = "${2:compartment_id}"
    index_name = "${3:index_name}"
    table_name_or_id = "${4:table_name_or_id}"
}