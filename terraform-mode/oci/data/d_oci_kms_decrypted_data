# -*- mode: snippet -*-
# name: d_oci_kms_decrypted_data
# key: d_oci_kms_decrypted_data
# --

data "oci_kms_decrypted_data" "${1:this}" {
    ciphertext = "${2:ciphertext}"
    crypto_endpoint = "${3:crypto_endpoint}"
    key_id = "${4:key_id}"
}