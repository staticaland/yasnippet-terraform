# -*- mode: snippet -*-
# name: d_oci_vault_secret_version
# key: d_oci_vault_secret_version
# --

data "oci_vault_secret_version" "${1:this}" {
    secret_id = "${2:secret_id}"
    secret_version_number = "${3:secret_version_number}"
}