# -*- mode: snippet -*-
# name: r_scaleway_volume_attachment
# key: r_scaleway_volume_attachment
# --

resource "scaleway_volume_attachment" "${1:this}" {
    server = "${2:server}"
    volume = "${3:volume}"
}