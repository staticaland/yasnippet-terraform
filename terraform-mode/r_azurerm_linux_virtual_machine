# -*- mode: snippet -*-
# name: r_azurerm_linux_virtual_machine
# key: r_azurerm_linux_virtual_machine
# --

resource "azurerm_linux_virtual_machine" "${1:this}" {
    admin_username = "${2:admin_username}"
    location = "${3:location}"
    name = "${4:name}"
    network_interface_ids = "${5:network_interface_ids}"
    resource_group_name = "${6:resource_group_name}"
    size = "${7:size}"
}