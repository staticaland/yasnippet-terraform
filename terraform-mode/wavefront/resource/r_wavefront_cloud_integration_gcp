# -*- mode: snippet -*-
# name: r_wavefront_cloud_integration_gcp
# key: r_wavefront_cloud_integration_gcp
# --

resource "wavefront_cloud_integration_gcp" "${1:this}" {
    json_key = "${2:json_key}"
    name = "${3:name}"
    project_id = "${4:project_id}"
}