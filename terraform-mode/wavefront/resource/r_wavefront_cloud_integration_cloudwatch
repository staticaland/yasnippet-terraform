# -*- mode: snippet -*-
# name: r_wavefront_cloud_integration_cloudwatch
# key: r_wavefront_cloud_integration_cloudwatch
# --

resource "wavefront_cloud_integration_cloudwatch" "${1:this}" {
    external_id = "${2:external_id}"
    name = "${3:name}"
    role_arn = "${4:role_arn}"
}