# -*- mode: snippet -*-
# name: r_pingaccess_site_authenticator
# key: r_pingaccess_site_authenticator
# --

resource "pingaccess_site_authenticator" "${1:this}" {
    class_name = "${2:class_name}"
    configuration = "${3:configuration}"
    name = "${4:name}"
}