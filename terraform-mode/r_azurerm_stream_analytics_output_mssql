# -*- mode: snippet -*-
# name: r_azurerm_stream_analytics_output_mssql
# key: r_azurerm_stream_analytics_output_mssql
# --

resource "azurerm_stream_analytics_output_mssql" "${1:this}" {
    database = "${2:database}"
    name = "${3:name}"
    password = "${4:password}"
    resource_group_name = "${5:resource_group_name}"
    server = "${6:server}"
    stream_analytics_job_name = "${7:stream_analytics_job_name}"
    table = "${8:table}"
    user = "${9:user}"
}