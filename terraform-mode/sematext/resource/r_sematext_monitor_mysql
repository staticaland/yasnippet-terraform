# -*- mode: snippet -*-
# name: r_sematext_monitor_mysql
# key: r_sematext_monitor_mysql
# --

resource "sematext_monitor_mysql" "${1:this}" {
    billing_plan_id = "${2:billing_plan_id}"
    name = "${3:name}"
}