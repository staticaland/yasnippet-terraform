# -*- mode: snippet -*-
# name: r_sematext_monitor_docker
# key: r_sematext_monitor_docker
# --

resource "sematext_monitor_docker" "${1:this}" {
    billing_plan_id = "${2:billing_plan_id}"
    name = "${3:name}"
}