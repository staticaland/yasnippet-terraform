# -*- mode: snippet -*-
# name: r_aws_opsworks_instance
# key: r_aws_opsworks_instance
# --

resource "aws_opsworks_instance" "${1:this}" {
    layer_ids = "${2:layer_ids}"
    stack_id = "${3:stack_id}"
}