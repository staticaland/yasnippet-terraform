# -*- mode: snippet -*-
# name: r_okta_password_policy_rule
# key: r_okta_password_policy_rule
# --

resource "okta_password_policy_rule" "${1:this}" {
    name = "${2:name}"
    policyid = "${3:policyid}"
}