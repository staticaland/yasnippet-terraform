# -*- mode: snippet -*-
# name: r_opentelekomcloud_cce_cluster_v3
# key: r_opentelekomcloud_cce_cluster_v3
# --

resource "opentelekomcloud_cce_cluster_v3" "${1:this}" {
    cluster_type = "${2:cluster_type}"
    container_network_type = "${3:container_network_type}"
    flavor_id = "${4:flavor_id}"
    name = "${5:name}"
    subnet_id = "${6:subnet_id}"
    vpc_id = "${7:vpc_id}"
}