# -*- mode: snippet -*-
# name: r_opentelekomcloud_elb_listener
# key: r_opentelekomcloud_elb_listener
# --

resource "opentelekomcloud_elb_listener" "${1:this}" {
    backend_port = "${2:backend_port}"
    backend_protocol = "${3:backend_protocol}"
    lb_algorithm = "${4:lb_algorithm}"
    loadbalancer_id = "${5:loadbalancer_id}"
    protocol = "${6:protocol}"
    protocol_port = "${7:protocol_port}"
}