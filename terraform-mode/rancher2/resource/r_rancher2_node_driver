# -*- mode: snippet -*-
# name: r_rancher2_node_driver
# key: r_rancher2_node_driver
# --

resource "rancher2_node_driver" "${1:this}" {
    active = "${2:active}"
    builtin = "${3:builtin}"
    name = "${4:name}"
    url = "${5:url}"
}