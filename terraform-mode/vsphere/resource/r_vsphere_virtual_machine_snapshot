# -*- mode: snippet -*-
# name: r_vsphere_virtual_machine_snapshot
# key: r_vsphere_virtual_machine_snapshot
# --

resource "vsphere_virtual_machine_snapshot" "${1:this}" {
    description = "${2:description}"
    memory = "${3:memory}"
    quiesce = "${4:quiesce}"
    snapshot_name = "${5:snapshot_name}"
    virtual_machine_uuid = "${6:virtual_machine_uuid}"
}