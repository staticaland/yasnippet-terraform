# -*- mode: snippet -*-
# name: r_vsphere_resource_pool
# key: r_vsphere_resource_pool
# --

resource "vsphere_resource_pool" "${1:this}" {
    name = "${2:name}"
    parent_resource_pool_id = "${3:parent_resource_pool_id}"
}