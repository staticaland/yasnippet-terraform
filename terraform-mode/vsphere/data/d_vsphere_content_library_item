# -*- mode: snippet -*-
# name: d_vsphere_content_library_item
# key: d_vsphere_content_library_item
# --

data "vsphere_content_library_item" "${1:this}" {
    library_id = "${2:library_id}"
    name = "${3:name}"
}