# -*- mode: snippet -*-
# name: r_heroku_formation
# key: r_heroku_formation
# --

resource "heroku_formation" "${1:this}" {
    app = "${2:app}"
    quantity = "${3:quantity}"
    size = "${4:size}"
    type = "${5:type}"
}