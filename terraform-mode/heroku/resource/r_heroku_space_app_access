# -*- mode: snippet -*-
# name: r_heroku_space_app_access
# key: r_heroku_space_app_access
# --

resource "heroku_space_app_access" "${1:this}" {
    email = "${2:email}"
    permissions = "${3:permissions}"
    space = "${4:space}"
}