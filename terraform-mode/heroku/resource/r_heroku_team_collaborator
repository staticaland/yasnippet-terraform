# -*- mode: snippet -*-
# name: r_heroku_team_collaborator
# key: r_heroku_team_collaborator
# --

resource "heroku_team_collaborator" "${1:this}" {
    app = "${2:app}"
    email = "${3:email}"
    permissions = "${4:permissions}"
}