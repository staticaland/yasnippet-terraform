# -*- mode: snippet -*-
# name: r_heroku_pipeline_coupling
# key: r_heroku_pipeline_coupling
# --

resource "heroku_pipeline_coupling" "${1:this}" {
    app = "${2:app}"
    pipeline = "${3:pipeline}"
    stage = "${4:stage}"
}