# -*- mode: snippet -*-
# name: r_constellix_tcp_check
# key: r_constellix_tcp_check
# --

resource "constellix_tcp_check" "${1:this}" {
    check_sites = "${2:check_sites}"
    host = "${3:host}"
    ip_version = "${4:ip_version}"
    port = "${5:port}"
}