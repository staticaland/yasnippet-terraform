# -*- mode: snippet -*-
# name: r_openstack_lb_vip_v1
# key: r_openstack_lb_vip_v1
# --

resource "openstack_lb_vip_v1" "${1:this}" {
    name = "${2:name}"
    pool_id = "${3:pool_id}"
    port = "${4:port}"
    protocol = "${5:protocol}"
    subnet_id = "${6:subnet_id}"
}