# -*- mode: snippet -*-
# name: r_openstack_lb_member_v1
# key: r_openstack_lb_member_v1
# --

resource "openstack_lb_member_v1" "${1:this}" {
    address = "${2:address}"
    pool_id = "${3:pool_id}"
    port = "${4:port}"
}