# -*- mode: snippet -*-
# name: r_openstack_lb_listener_v2
# key: r_openstack_lb_listener_v2
# --

resource "openstack_lb_listener_v2" "${1:this}" {
    loadbalancer_id = "${2:loadbalancer_id}"
    protocol = "${3:protocol}"
    protocol_port = "${4:protocol_port}"
}