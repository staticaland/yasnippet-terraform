# -*- mode: snippet -*-
# name: r_panos_bgp_aggregate
# key: r_panos_bgp_aggregate
# --

resource "panos_bgp_aggregate" "${1:this}" {
    name = "${2:name}"
    prefix = "${3:prefix}"
    virtual_router = "${4:virtual_router}"
}