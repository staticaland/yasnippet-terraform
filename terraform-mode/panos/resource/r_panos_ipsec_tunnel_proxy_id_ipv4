# -*- mode: snippet -*-
# name: r_panos_ipsec_tunnel_proxy_id_ipv4
# key: r_panos_ipsec_tunnel_proxy_id_ipv4
# --

resource "panos_ipsec_tunnel_proxy_id_ipv4" "${1:this}" {
    ipsec_tunnel = "${2:ipsec_tunnel}"
    name = "${3:name}"
}