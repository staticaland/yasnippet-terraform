# -*- mode: snippet -*-
# name: r_panos_bgp_redist_rule
# key: r_panos_bgp_redist_rule
# --

resource "panos_bgp_redist_rule" "${1:this}" {
    name = "${2:name}"
    virtual_router = "${3:virtual_router}"
}