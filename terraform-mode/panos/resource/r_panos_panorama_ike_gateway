# -*- mode: snippet -*-
# name: r_panos_panorama_ike_gateway
# key: r_panos_panorama_ike_gateway
# --

resource "panos_panorama_ike_gateway" "${1:this}" {
    interface = "${2:interface}"
    name = "${3:name}"
}