# -*- mode: snippet -*-
# name: r_panos_panorama_tunnel_interface
# key: r_panos_panorama_tunnel_interface
# --

resource "panos_panorama_tunnel_interface" "${1:this}" {
    name = "${2:name}"
    template = "${3:template}"
}