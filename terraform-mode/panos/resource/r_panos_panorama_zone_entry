# -*- mode: snippet -*-
# name: r_panos_panorama_zone_entry
# key: r_panos_panorama_zone_entry
# --

resource "panos_panorama_zone_entry" "${1:this}" {
    interface = "${2:interface}"
    template = "${3:template}"
    zone = "${4:zone}"
}