# -*- mode: snippet -*-
# name: r_panos_panorama_loopback_interface
# key: r_panos_panorama_loopback_interface
# --

resource "panos_panorama_loopback_interface" "${1:this}" {
    name = "${2:name}"
    template = "${3:template}"
}