# -*- mode: snippet -*-
# name: r_panos_panorama_service_group
# key: r_panos_panorama_service_group
# --

resource "panos_panorama_service_group" "${1:this}" {
    name = "${2:name}"
    services = "${3:services}"
}