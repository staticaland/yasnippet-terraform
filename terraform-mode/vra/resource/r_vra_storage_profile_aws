# -*- mode: snippet -*-
# name: r_vra_storage_profile_aws
# key: r_vra_storage_profile_aws
# --

resource "vra_storage_profile_aws" "${1:this}" {
    default_item = "${2:default_item}"
    name = "${3:name}"
    region_id = "${4:region_id}"
}