# -*- mode: snippet -*-
# name: r_vra_blueprint_version
# key: r_vra_blueprint_version
# --

resource "vra_blueprint_version" "${1:this}" {
    blueprint_id = "${2:blueprint_id}"
    version = "${3:version}"
}