# -*- mode: snippet -*-
# name: r_azurerm_automation_certificate
# key: r_azurerm_automation_certificate
# --

resource "azurerm_automation_certificate" "${1:this}" {
    automation_account_name = "${2:automation_account_name}"
    base64 = "${3:base64}"
    name = "${4:name}"
    resource_group_name = "${5:resource_group_name}"
}