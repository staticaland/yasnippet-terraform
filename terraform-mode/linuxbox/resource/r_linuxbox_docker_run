# -*- mode: snippet -*-
# name: r_linuxbox_docker_run
# key: r_linuxbox_docker_run
# --

resource "linuxbox_docker_run" "${1:this}" {
    host_address = "${2:host_address}"
    image_id = "${3:image_id}"
    ssh_key = "${4:ssh_key}"
}