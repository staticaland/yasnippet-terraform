# -*- mode: snippet -*-
# name: r_google_endpoints_service_iam_binding
# key: r_google_endpoints_service_iam_binding
# --

resource "google_endpoints_service_iam_binding" "${1:this}" {
    members = "${2:members}"
    role = "${3:role}"
    service_name = "${4:service_name}"
}