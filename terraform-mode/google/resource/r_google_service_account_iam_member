# -*- mode: snippet -*-
# name: r_google_service_account_iam_member
# key: r_google_service_account_iam_member
# --

resource "google_service_account_iam_member" "${1:this}" {
    member = "${2:member}"
    role = "${3:role}"
    service_account_id = "${4:service_account_id}"
}