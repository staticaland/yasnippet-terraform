# -*- mode: snippet -*-
# name: r_google_iap_app_engine_version_iam_binding
# key: r_google_iap_app_engine_version_iam_binding
# --

resource "google_iap_app_engine_version_iam_binding" "${1:this}" {
    app_id = "${2:app_id}"
    members = "${3:members}"
    role = "${4:role}"
    service = "${5:service}"
    version_id = "${6:version_id}"
}