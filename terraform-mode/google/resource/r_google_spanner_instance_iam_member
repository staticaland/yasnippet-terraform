# -*- mode: snippet -*-
# name: r_google_spanner_instance_iam_member
# key: r_google_spanner_instance_iam_member
# --

resource "google_spanner_instance_iam_member" "${1:this}" {
    instance = "${2:instance}"
    member = "${3:member}"
    role = "${4:role}"
}