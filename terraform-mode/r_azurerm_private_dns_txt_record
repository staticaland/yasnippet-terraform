# -*- mode: snippet -*-
# name: r_azurerm_private_dns_txt_record
# key: r_azurerm_private_dns_txt_record
# --

resource "azurerm_private_dns_txt_record" "${1:this}" {
    name = "${2:name}"
    resource_group_name = "${3:resource_group_name}"
    ttl = "${4:ttl}"
    zone_name = "${5:zone_name}"
}