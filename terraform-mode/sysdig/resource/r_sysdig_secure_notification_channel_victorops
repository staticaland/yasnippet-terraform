# -*- mode: snippet -*-
# name: r_sysdig_secure_notification_channel_victorops
# key: r_sysdig_secure_notification_channel_victorops
# --

resource "sysdig_secure_notification_channel_victorops" "${1:this}" {
    api_key = "${2:api_key}"
    enabled = "${3:enabled}"
    name = "${4:name}"
    notify_when_ok = "${5:notify_when_ok}"
    notify_when_resolved = "${6:notify_when_resolved}"
    routing_key = "${7:routing_key}"
}