# -*- mode: snippet -*-
# name: r_sysdig_secure_notification_channel_email
# key: r_sysdig_secure_notification_channel_email
# --

resource "sysdig_secure_notification_channel_email" "${1:this}" {
    enabled = "${2:enabled}"
    name = "${3:name}"
    notify_when_ok = "${4:notify_when_ok}"
    notify_when_resolved = "${5:notify_when_resolved}"
    recipients = "${6:recipients}"
}