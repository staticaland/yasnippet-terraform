# -*- mode: snippet -*-
# name: r_google_compute_global_forwarding_rule
# key: r_google_compute_global_forwarding_rule
# --

resource "google_compute_global_forwarding_rule" "${1:this}" {
    name = "${2:name}"
    target = "${3:target}"
}