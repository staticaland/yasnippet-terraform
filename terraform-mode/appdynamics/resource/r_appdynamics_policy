# -*- mode: snippet -*-
# name: r_appdynamics_policy
# key: r_appdynamics_policy
# --

resource "appdynamics_policy" "${1:this}" {
    action_name = "${2:action_name}"
    action_type = "${3:action_type}"
    application_id = "${4:application_id}"
    health_rule_event_types = "${5:health_rule_event_types}"
    health_rule_scope_type = "${6:health_rule_scope_type}"
    name = "${7:name}"
}