# -*- mode: snippet -*-
# name: r_appdynamics_action
# key: r_appdynamics_action
# --

resource "appdynamics_action" "${1:this}" {
    action_type = "${2:action_type}"
    application_id = "${3:application_id}"
}