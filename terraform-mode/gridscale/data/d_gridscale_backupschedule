# -*- mode: snippet -*-
# name: d_gridscale_backupschedule
# key: d_gridscale_backupschedule
# --

data "gridscale_backupschedule" "${1:this}" {
    resource_id = "${2:resource_id}"
    storage_uuid = "${3:storage_uuid}"
}