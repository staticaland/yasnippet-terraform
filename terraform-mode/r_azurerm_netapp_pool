# -*- mode: snippet -*-
# name: r_azurerm_netapp_pool
# key: r_azurerm_netapp_pool
# --

resource "azurerm_netapp_pool" "${1:this}" {
    account_name = "${2:account_name}"
    location = "${3:location}"
    name = "${4:name}"
    resource_group_name = "${5:resource_group_name}"
    service_level = "${6:service_level}"
    size_in_tb = "${7:size_in_tb}"
}