# -*- mode: snippet -*-
# name: r_oraclepaas_mysql_access_rule
# key: r_oraclepaas_mysql_access_rule
# --

resource "oraclepaas_mysql_access_rule" "${1:this}" {
    destination = "${2:destination}"
    name = "${3:name}"
    ports = "${4:ports}"
    service_instance_id = "${5:service_instance_id}"
    source = "${6:source}"
}