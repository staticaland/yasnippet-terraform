# -*- mode: snippet -*-
# name: r_azurerm_site_recovery_protection_container
# key: r_azurerm_site_recovery_protection_container
# --

resource "azurerm_site_recovery_protection_container" "${1:this}" {
    name = "${2:name}"
    recovery_fabric_name = "${3:recovery_fabric_name}"
    recovery_vault_name = "${4:recovery_vault_name}"
    resource_group_name = "${5:resource_group_name}"
}