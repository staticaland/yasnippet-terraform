# -*- mode: snippet -*-
# name: r_ibm_resource_instance
# key: r_ibm_resource_instance
# --

resource "ibm_resource_instance" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    plan = "${4:plan}"
    service = "${5:service}"
}