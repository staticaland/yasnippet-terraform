# -*- mode: snippet -*-
# name: r_ibm_is_ipsec_policy
# key: r_ibm_is_ipsec_policy
# --

resource "ibm_is_ipsec_policy" "${1:this}" {
    authentication_algorithm = "${2:authentication_algorithm}"
    encryption_algorithm = "${3:encryption_algorithm}"
    name = "${4:name}"
    pfs = "${5:pfs}"
}