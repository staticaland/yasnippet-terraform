# -*- mode: snippet -*-
# name: r_ibm_container_cluster
# key: r_ibm_container_cluster
# --

resource "ibm_container_cluster" "${1:this}" {
    datacenter = "${2:datacenter}"
    hardware = "${3:hardware}"
    name = "${4:name}"
}