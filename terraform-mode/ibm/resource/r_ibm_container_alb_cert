# -*- mode: snippet -*-
# name: r_ibm_container_alb_cert
# key: r_ibm_container_alb_cert
# --

resource "ibm_container_alb_cert" "${1:this}" {
    cert_crn = "${2:cert_crn}"
    cluster_id = "${3:cluster_id}"
    secret_name = "${4:secret_name}"
}