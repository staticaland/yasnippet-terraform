# -*- mode: snippet -*-
# name: r_ibm_cis_firewall
# key: r_ibm_cis_firewall
# --

resource "ibm_cis_firewall" "${1:this}" {
    cis_id = "${2:cis_id}"
    domain_id = "${3:domain_id}"
    firewall_type = "${4:firewall_type}"
}