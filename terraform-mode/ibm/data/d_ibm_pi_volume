# -*- mode: snippet -*-
# name: d_ibm_pi_volume
# key: d_ibm_pi_volume
# --

data "ibm_pi_volume" "${1:this}" {
    pi_cloud_instance_id = "${2:pi_cloud_instance_id}"
    pi_volume_name = "${3:pi_volume_name}"
}