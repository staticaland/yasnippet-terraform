# -*- mode: snippet -*-
# name: d_ibm_compute_vm_instance
# key: d_ibm_compute_vm_instance
# --

data "ibm_compute_vm_instance" "${1:this}" {
    domain = "${2:domain}"
    hostname = "${3:hostname}"
}