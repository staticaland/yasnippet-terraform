# -*- mode: snippet -*-
# name: d_ibm_pi_instance
# key: d_ibm_pi_instance
# --

data "ibm_pi_instance" "${1:this}" {
    pi_cloud_instance_id = "${2:pi_cloud_instance_id}"
    pi_instance_name = "${3:pi_instance_name}"
}