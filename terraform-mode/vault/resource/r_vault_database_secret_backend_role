# -*- mode: snippet -*-
# name: r_vault_database_secret_backend_role
# key: r_vault_database_secret_backend_role
# --

resource "vault_database_secret_backend_role" "${1:this}" {
    backend = "${2:backend}"
    creation_statements = "${3:creation_statements}"
    db_name = "${4:db_name}"
    name = "${5:name}"
}