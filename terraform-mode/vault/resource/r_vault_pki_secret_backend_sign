# -*- mode: snippet -*-
# name: r_vault_pki_secret_backend_sign
# key: r_vault_pki_secret_backend_sign
# --

resource "vault_pki_secret_backend_sign" "${1:this}" {
    backend = "${2:backend}"
    common_name = "${3:common_name}"
    csr = "${4:csr}"
    name = "${5:name}"
}