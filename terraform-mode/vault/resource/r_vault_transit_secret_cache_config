# -*- mode: snippet -*-
# name: r_vault_transit_secret_cache_config
# key: r_vault_transit_secret_cache_config
# --

resource "vault_transit_secret_cache_config" "${1:this}" {
    backend = "${2:backend}"
    size = "${3:size}"
}