# -*- mode: snippet -*-
# name: r_vault_pki_secret_backend_role
# key: r_vault_pki_secret_backend_role
# --

resource "vault_pki_secret_backend_role" "${1:this}" {
    backend = "${2:backend}"
    name = "${3:name}"
}