# -*- mode: snippet -*-
# name: r_vault_pki_secret_backend_root_sign_intermediate
# key: r_vault_pki_secret_backend_root_sign_intermediate
# --

resource "vault_pki_secret_backend_root_sign_intermediate" "${1:this}" {
    backend = "${2:backend}"
    common_name = "${3:common_name}"
    csr = "${4:csr}"
}