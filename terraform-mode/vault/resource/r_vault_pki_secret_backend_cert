# -*- mode: snippet -*-
# name: r_vault_pki_secret_backend_cert
# key: r_vault_pki_secret_backend_cert
# --

resource "vault_pki_secret_backend_cert" "${1:this}" {
    backend = "${2:backend}"
    common_name = "${3:common_name}"
    name = "${4:name}"
}