# -*- mode: snippet -*-
# name: r_vault_rgp_policy
# key: r_vault_rgp_policy
# --

resource "vault_rgp_policy" "${1:this}" {
    enforcement_level = "${2:enforcement_level}"
    name = "${3:name}"
    policy = "${4:policy}"
}