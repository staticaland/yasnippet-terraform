# -*- mode: snippet -*-
# name: r_vault_identity_group_alias
# key: r_vault_identity_group_alias
# --

resource "vault_identity_group_alias" "${1:this}" {
    canonical_id = "${2:canonical_id}"
    mount_accessor = "${3:mount_accessor}"
    name = "${4:name}"
}