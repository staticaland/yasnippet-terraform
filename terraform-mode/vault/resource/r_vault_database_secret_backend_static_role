# -*- mode: snippet -*-
# name: r_vault_database_secret_backend_static_role
# key: r_vault_database_secret_backend_static_role
# --

resource "vault_database_secret_backend_static_role" "${1:this}" {
    backend = "${2:backend}"
    db_name = "${3:db_name}"
    name = "${4:name}"
    rotation_period = "${5:rotation_period}"
    username = "${6:username}"
}