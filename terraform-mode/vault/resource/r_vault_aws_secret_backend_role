# -*- mode: snippet -*-
# name: r_vault_aws_secret_backend_role
# key: r_vault_aws_secret_backend_role
# --

resource "vault_aws_secret_backend_role" "${1:this}" {
    backend = "${2:backend}"
    credential_type = "${3:credential_type}"
    name = "${4:name}"
}