# -*- mode: snippet -*-
# name: r_commercetools_type
# key: r_commercetools_type
# --

resource "commercetools_type" "${1:this}" {
    key = "${2:key}"
    name = "${3:name}"
    resource_type_ids = "${4:resource_type_ids}"
}