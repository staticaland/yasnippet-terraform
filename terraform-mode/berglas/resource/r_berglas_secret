# -*- mode: snippet -*-
# name: r_berglas_secret
# key: r_berglas_secret
# --

resource "berglas_secret" "${1:this}" {
    bucket = "${2:bucket}"
    key = "${3:key}"
    name = "${4:name}"
    plaintext = "${5:plaintext}"
}