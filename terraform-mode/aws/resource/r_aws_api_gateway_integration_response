# -*- mode: snippet -*-
# name: r_aws_api_gateway_integration_response
# key: r_aws_api_gateway_integration_response
# --

resource "aws_api_gateway_integration_response" "${1:this}" {
    http_method = "${2:http_method}"
    resource_id = "${3:resource_id}"
    rest_api_id = "${4:rest_api_id}"
    status_code = "${5:status_code}"
}