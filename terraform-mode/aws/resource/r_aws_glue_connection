# -*- mode: snippet -*-
# name: r_aws_glue_connection
# key: r_aws_glue_connection
# --

resource "aws_glue_connection" "${1:this}" {
    connection_properties = "${2:connection_properties}"
    name = "${3:name}"
}