# -*- mode: snippet -*-
# name: r_aws_iam_server_certificate
# key: r_aws_iam_server_certificate
# --

resource "aws_iam_server_certificate" "${1:this}" {
    certificate_body = "${2:certificate_body}"
    private_key = "${3:private_key}"
}