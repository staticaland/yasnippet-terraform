# -*- mode: snippet -*-
# name: r_aws_inspector_assessment_template
# key: r_aws_inspector_assessment_template
# --

resource "aws_inspector_assessment_template" "${1:this}" {
    duration = "${2:duration}"
    name = "${3:name}"
    rules_package_arns = "${4:rules_package_arns}"
    target_arn = "${5:target_arn}"
}