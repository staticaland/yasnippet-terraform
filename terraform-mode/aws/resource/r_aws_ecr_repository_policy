# -*- mode: snippet -*-
# name: r_aws_ecr_repository_policy
# key: r_aws_ecr_repository_policy
# --

resource "aws_ecr_repository_policy" "${1:this}" {
    policy = "${2:policy}"
    repository = "${3:repository}"
}