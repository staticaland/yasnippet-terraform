# -*- mode: snippet -*-
# name: r_aws_vpc_endpoint_subnet_association
# key: r_aws_vpc_endpoint_subnet_association
# --

resource "aws_vpc_endpoint_subnet_association" "${1:this}" {
    subnet_id = "${2:subnet_id}"
    vpc_endpoint_id = "${3:vpc_endpoint_id}"
}