# -*- mode: snippet -*-
# name: r_aws_redshift_subnet_group
# key: r_aws_redshift_subnet_group
# --

resource "aws_redshift_subnet_group" "${1:this}" {
    name = "${2:name}"
    subnet_ids = "${3:subnet_ids}"
}