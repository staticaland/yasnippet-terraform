# -*- mode: snippet -*-
# name: r_aws_apigatewayv2_authorizer
# key: r_aws_apigatewayv2_authorizer
# --

resource "aws_apigatewayv2_authorizer" "${1:this}" {
    api_id = "${2:api_id}"
    authorizer_type = "${3:authorizer_type}"
    identity_sources = "${4:identity_sources}"
    name = "${5:name}"
}