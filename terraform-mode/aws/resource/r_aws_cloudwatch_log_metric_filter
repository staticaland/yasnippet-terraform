# -*- mode: snippet -*-
# name: r_aws_cloudwatch_log_metric_filter
# key: r_aws_cloudwatch_log_metric_filter
# --

resource "aws_cloudwatch_log_metric_filter" "${1:this}" {
    log_group_name = "${2:log_group_name}"
    name = "${3:name}"
    pattern = "${4:pattern}"
}