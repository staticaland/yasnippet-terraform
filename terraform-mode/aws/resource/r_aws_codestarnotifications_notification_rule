# -*- mode: snippet -*-
# name: r_aws_codestarnotifications_notification_rule
# key: r_aws_codestarnotifications_notification_rule
# --

resource "aws_codestarnotifications_notification_rule" "${1:this}" {
    detail_type = "${2:detail_type}"
    event_type_ids = "${3:event_type_ids}"
    name = "${4:name}"
    resource = "${5:resource}"
}