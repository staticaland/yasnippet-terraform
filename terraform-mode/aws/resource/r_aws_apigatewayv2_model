# -*- mode: snippet -*-
# name: r_aws_apigatewayv2_model
# key: r_aws_apigatewayv2_model
# --

resource "aws_apigatewayv2_model" "${1:this}" {
    api_id = "${2:api_id}"
    content_type = "${3:content_type}"
    name = "${4:name}"
    schema = "${5:schema}"
}