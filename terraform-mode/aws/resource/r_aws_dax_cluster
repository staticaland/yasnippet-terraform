# -*- mode: snippet -*-
# name: r_aws_dax_cluster
# key: r_aws_dax_cluster
# --

resource "aws_dax_cluster" "${1:this}" {
    cluster_name = "${2:cluster_name}"
    iam_role_arn = "${3:iam_role_arn}"
    node_type = "${4:node_type}"
    replication_factor = "${5:replication_factor}"
}