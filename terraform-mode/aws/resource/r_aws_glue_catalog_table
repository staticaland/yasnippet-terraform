# -*- mode: snippet -*-
# name: r_aws_glue_catalog_table
# key: r_aws_glue_catalog_table
# --

resource "aws_glue_catalog_table" "${1:this}" {
    database_name = "${2:database_name}"
    name = "${3:name}"
}