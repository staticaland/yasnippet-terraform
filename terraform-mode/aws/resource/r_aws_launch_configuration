# -*- mode: snippet -*-
# name: r_aws_launch_configuration
# key: r_aws_launch_configuration
# --

resource "aws_launch_configuration" "${1:this}" {
    image_id = "${2:image_id}"
    instance_type = "${3:instance_type}"
}