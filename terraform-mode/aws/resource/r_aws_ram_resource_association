# -*- mode: snippet -*-
# name: r_aws_ram_resource_association
# key: r_aws_ram_resource_association
# --

resource "aws_ram_resource_association" "${1:this}" {
    resource_arn = "${2:resource_arn}"
    resource_share_arn = "${3:resource_share_arn}"
}