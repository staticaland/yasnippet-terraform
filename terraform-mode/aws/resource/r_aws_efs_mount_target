# -*- mode: snippet -*-
# name: r_aws_efs_mount_target
# key: r_aws_efs_mount_target
# --

resource "aws_efs_mount_target" "${1:this}" {
    file_system_id = "${2:file_system_id}"
    subnet_id = "${3:subnet_id}"
}