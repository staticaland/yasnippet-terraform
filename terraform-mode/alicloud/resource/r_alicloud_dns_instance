# -*- mode: snippet -*-
# name: r_alicloud_dns_instance
# key: r_alicloud_dns_instance
# --

resource "alicloud_dns_instance" "${1:this}" {
    dns_security = "${2:dns_security}"
    domain_numbers = "${3:domain_numbers}"
    version_code = "${4:version_code}"
}