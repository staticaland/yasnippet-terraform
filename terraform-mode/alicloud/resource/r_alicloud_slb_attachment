# -*- mode: snippet -*-
# name: r_alicloud_slb_attachment
# key: r_alicloud_slb_attachment
# --

resource "alicloud_slb_attachment" "${1:this}" {
    instance_ids = "${2:instance_ids}"
    load_balancer_id = "${3:load_balancer_id}"
}