# -*- mode: snippet -*-
# name: r_alicloud_db_readonly_instance
# key: r_alicloud_db_readonly_instance
# --

resource "alicloud_db_readonly_instance" "${1:this}" {
    engine_version = "${2:engine_version}"
    instance_storage = "${3:instance_storage}"
    instance_type = "${4:instance_type}"
    master_db_instance_id = "${5:master_db_instance_id}"
}