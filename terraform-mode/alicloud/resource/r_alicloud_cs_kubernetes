# -*- mode: snippet -*-
# name: r_alicloud_cs_kubernetes
# key: r_alicloud_cs_kubernetes
# --

resource "alicloud_cs_kubernetes" "${1:this}" {
    master_instance_types = "${2:master_instance_types}"
    master_vswitch_ids = "${3:master_vswitch_ids}"
    worker_instance_types = "${4:worker_instance_types}"
    worker_number = "${5:worker_number}"
    worker_vswitch_ids = "${6:worker_vswitch_ids}"
}