# -*- mode: snippet -*-
# name: r_alicloud_instance
# key: r_alicloud_instance
# --

resource "alicloud_instance" "${1:this}" {
    image_id = "${2:image_id}"
    instance_type = "${3:instance_type}"
    security_groups = "${4:security_groups}"
}