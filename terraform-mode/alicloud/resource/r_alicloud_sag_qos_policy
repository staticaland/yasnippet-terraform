# -*- mode: snippet -*-
# name: r_alicloud_sag_qos_policy
# key: r_alicloud_sag_qos_policy
# --

resource "alicloud_sag_qos_policy" "${1:this}" {
    dest_cidr = "${2:dest_cidr}"
    dest_port_range = "${3:dest_port_range}"
    ip_protocol = "${4:ip_protocol}"
    priority = "${5:priority}"
    qos_id = "${6:qos_id}"
    source_cidr = "${7:source_cidr}"
    source_port_range = "${8:source_port_range}"
}