# -*- mode: snippet -*-
# name: r_alicloud_cdn_domain
# key: r_alicloud_cdn_domain
# --

resource "alicloud_cdn_domain" "${1:this}" {
    cdn_type = "${2:cdn_type}"
    domain_name = "${3:domain_name}"
}