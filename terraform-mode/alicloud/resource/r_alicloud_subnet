# -*- mode: snippet -*-
# name: r_alicloud_subnet
# key: r_alicloud_subnet
# --

resource "alicloud_subnet" "${1:this}" {
    availability_zone = "${2:availability_zone}"
    cidr_block = "${3:cidr_block}"
    vpc_id = "${4:vpc_id}"
}