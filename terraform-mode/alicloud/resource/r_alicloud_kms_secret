# -*- mode: snippet -*-
# name: r_alicloud_kms_secret
# key: r_alicloud_kms_secret
# --

resource "alicloud_kms_secret" "${1:this}" {
    secret_data = "${2:secret_data}"
    secret_name = "${3:secret_name}"
    version_id = "${4:version_id}"
}