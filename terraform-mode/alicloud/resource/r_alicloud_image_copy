# -*- mode: snippet -*-
# name: r_alicloud_image_copy
# key: r_alicloud_image_copy
# --

resource "alicloud_image_copy" "${1:this}" {
    source_image_id = "${2:source_image_id}"
    source_region_id = "${3:source_region_id}"
}