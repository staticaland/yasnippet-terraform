# -*- mode: snippet -*-
# name: r_alicloud_havip_attachment
# key: r_alicloud_havip_attachment
# --

resource "alicloud_havip_attachment" "${1:this}" {
    havip_id = "${2:havip_id}"
    instance_id = "${3:instance_id}"
}