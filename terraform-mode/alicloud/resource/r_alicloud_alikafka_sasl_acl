# -*- mode: snippet -*-
# name: r_alicloud_alikafka_sasl_acl
# key: r_alicloud_alikafka_sasl_acl
# --

resource "alicloud_alikafka_sasl_acl" "${1:this}" {
    acl_operation_type = "${2:acl_operation_type}"
    acl_resource_name = "${3:acl_resource_name}"
    acl_resource_pattern_type = "${4:acl_resource_pattern_type}"
    acl_resource_type = "${5:acl_resource_type}"
    instance_id = "${6:instance_id}"
    username = "${7:username}"
}