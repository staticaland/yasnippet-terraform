# -*- mode: snippet -*-
# name: r_alicloud_api_gateway_app_attachment
# key: r_alicloud_api_gateway_app_attachment
# --

resource "alicloud_api_gateway_app_attachment" "${1:this}" {
    api_id = "${2:api_id}"
    app_id = "${3:app_id}"
    group_id = "${4:group_id}"
    stage_name = "${5:stage_name}"
}