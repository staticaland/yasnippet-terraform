# -*- mode: snippet -*-
# name: r_alicloud_container_cluster
# key: r_alicloud_container_cluster
# --

resource "alicloud_container_cluster" "${1:this}" {
    cidr_block = "${2:cidr_block}"
    instance_type = "${3:instance_type}"
    password = "${4:password}"
    vswitch_id = "${5:vswitch_id}"
}