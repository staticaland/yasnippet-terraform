# -*- mode: snippet -*-
# name: r_gitlab_instance_cluster
# key: r_gitlab_instance_cluster
# --

resource "gitlab_instance_cluster" "${1:this}" {
    kubernetes_api_url = "${2:kubernetes_api_url}"
    kubernetes_token = "${3:kubernetes_token}"
    name = "${4:name}"
}