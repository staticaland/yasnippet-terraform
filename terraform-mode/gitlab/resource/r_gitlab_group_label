# -*- mode: snippet -*-
# name: r_gitlab_group_label
# key: r_gitlab_group_label
# --

resource "gitlab_group_label" "${1:this}" {
    color = "${2:color}"
    group = "${3:group}"
    name = "${4:name}"
}