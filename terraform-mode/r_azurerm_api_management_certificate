
# -*- mode: snippet -*-
# name: r_azurerm_api_management_certificate
# key: r_azurerm_api_management_certificate
# --

resource "azurerm_api_management_certificate" "${1:this}" {
    api_management_name = "${2:api_management_name}"
    data = "${3:data}"
    name = "${4:name}"
    resource_group_name = "${5:resource_group_name}"
}