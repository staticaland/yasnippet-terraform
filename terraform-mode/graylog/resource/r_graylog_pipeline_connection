# -*- mode: snippet -*-
# name: r_graylog_pipeline_connection
# key: r_graylog_pipeline_connection
# --

resource "graylog_pipeline_connection" "${1:this}" {
    pipeline_ids = "${2:pipeline_ids}"
    stream_id = "${3:stream_id}"
}