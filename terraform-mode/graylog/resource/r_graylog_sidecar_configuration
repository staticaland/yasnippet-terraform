# -*- mode: snippet -*-
# name: r_graylog_sidecar_configuration
# key: r_graylog_sidecar_configuration
# --

resource "graylog_sidecar_configuration" "${1:this}" {
    collector_id = "${2:collector_id}"
    color = "${3:color}"
    name = "${4:name}"
    template = "${5:template}"
}