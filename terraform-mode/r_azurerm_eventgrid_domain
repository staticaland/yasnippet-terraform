# -*- mode: snippet -*-
# name: r_azurerm_eventgrid_domain
# key: r_azurerm_eventgrid_domain
# --

resource "azurerm_eventgrid_domain" "${1:this}" {
    location = "${2:location}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
}