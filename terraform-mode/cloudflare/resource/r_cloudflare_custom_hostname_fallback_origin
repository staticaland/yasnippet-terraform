# -*- mode: snippet -*-
# name: r_cloudflare_custom_hostname_fallback_origin
# key: r_cloudflare_custom_hostname_fallback_origin
# --

resource "cloudflare_custom_hostname_fallback_origin" "${1:this}" {
    origin = "${2:origin}"
    zone_id = "${3:zone_id}"
}