# -*- mode: snippet -*-
# name: r_cloudflare_record
# key: r_cloudflare_record
# --

resource "cloudflare_record" "${1:this}" {
    name = "${2:name}"
    type = "${3:type}"
    zone_id = "${4:zone_id}"
}