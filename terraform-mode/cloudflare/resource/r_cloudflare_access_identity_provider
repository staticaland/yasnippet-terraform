# -*- mode: snippet -*-
# name: r_cloudflare_access_identity_provider
# key: r_cloudflare_access_identity_provider
# --

resource "cloudflare_access_identity_provider" "${1:this}" {
    account_id = "${2:account_id}"
    name = "${3:name}"
    type = "${4:type}"
}