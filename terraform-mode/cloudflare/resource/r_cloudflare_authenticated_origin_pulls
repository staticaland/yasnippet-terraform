# -*- mode: snippet -*-
# name: r_cloudflare_authenticated_origin_pulls
# key: r_cloudflare_authenticated_origin_pulls
# --

resource "cloudflare_authenticated_origin_pulls" "${1:this}" {
    enabled = "${2:enabled}"
    zone_id = "${3:zone_id}"
}