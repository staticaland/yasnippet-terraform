# -*- mode: snippet -*-
# name: r_azurerm_api_management_api_version_set
# key: r_azurerm_api_management_api_version_set
# --

resource "azurerm_api_management_api_version_set" "${1:this}" {
    api_management_name = "${2:api_management_name}"
    display_name = "${3:display_name}"
    name = "${4:name}"
    resource_group_name = "${5:resource_group_name}"
    versioning_scheme = "${6:versioning_scheme}"
}