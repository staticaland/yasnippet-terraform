# -*- mode: snippet -*-
# name: r_idm_dns_record
# key: r_idm_dns_record
# --

resource "idm_dns_record" "${1:this}" {
    idm_dns_name = "${2:idm_dns_name}"
    idm_dns_zone_name = "${3:idm_dns_zone_name}"
    records = "${4:records}"
    type = "${5:type}"
}