# -*- mode: snippet -*-
# name: r_snowflake_stage
# key: r_snowflake_stage
# --

resource "snowflake_stage" "${1:this}" {
    database = "${2:database}"
    name = "${3:name}"
    schema = "${4:schema}"
}