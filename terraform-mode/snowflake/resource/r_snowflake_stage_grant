# -*- mode: snippet -*-
# name: r_snowflake_stage_grant
# key: r_snowflake_stage_grant
# --

resource "snowflake_stage_grant" "${1:this}" {
    database_name = "${2:database_name}"
    schema_name = "${3:schema_name}"
    stage_name = "${4:stage_name}"
}