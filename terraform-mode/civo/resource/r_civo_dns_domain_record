# -*- mode: snippet -*-
# name: r_civo_dns_domain_record
# key: r_civo_dns_domain_record
# --

resource "civo_dns_domain_record" "${1:this}" {
    domain_id = "${2:domain_id}"
    name = "${3:name}"
    ttl = "${4:ttl}"
    type = "${5:type}"
    value = "${6:value}"
}