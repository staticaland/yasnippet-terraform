# -*- mode: snippet -*-
# name: r_artifactory_remote_repository
# key: r_artifactory_remote_repository
# --

resource "artifactory_remote_repository" "${1:this}" {
    key = "${2:key}"
    url = "${3:url}"
}