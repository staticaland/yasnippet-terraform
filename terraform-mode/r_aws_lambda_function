# -*- mode: snippet -*-
# name: r_aws_lambda_function
# key: r_aws_lambda_function
# --

resource "aws_lambda_function" "${1:this}" {
    function_name = "${2:function_name}"
    handler = "${3:handler}"
    role = "${4:role}"
    runtime = "${5:runtime}"
}