# -*- mode: snippet -*-
# name: r_njalla_record_mx
# key: r_njalla_record_mx
# --

resource "njalla_record_mx" "${1:this}" {
    content = "${2:content}"
    domain = "${3:domain}"
    priority = "${4:priority}"
    ttl = "${5:ttl}"
}