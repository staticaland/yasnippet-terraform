# -*- mode: snippet -*-
# name: r_njalla_record_txt
# key: r_njalla_record_txt
# --

resource "njalla_record_txt" "${1:this}" {
    content = "${2:content}"
    domain = "${3:domain}"
    ttl = "${4:ttl}"
}