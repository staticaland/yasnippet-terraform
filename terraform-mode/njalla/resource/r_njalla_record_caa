# -*- mode: snippet -*-
# name: r_njalla_record_caa
# key: r_njalla_record_caa
# --

resource "njalla_record_caa" "${1:this}" {
    content = "${2:content}"
    domain = "${3:domain}"
    ttl = "${4:ttl}"
}