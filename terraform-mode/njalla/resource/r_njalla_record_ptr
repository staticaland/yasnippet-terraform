# -*- mode: snippet -*-
# name: r_njalla_record_ptr
# key: r_njalla_record_ptr
# --

resource "njalla_record_ptr" "${1:this}" {
    content = "${2:content}"
    domain = "${3:domain}"
    ttl = "${4:ttl}"
}