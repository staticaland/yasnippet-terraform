# -*- mode: snippet -*-
# name: r_njalla_record_naptr
# key: r_njalla_record_naptr
# --

resource "njalla_record_naptr" "${1:this}" {
    content = "${2:content}"
    domain = "${3:domain}"
    ttl = "${4:ttl}"
}