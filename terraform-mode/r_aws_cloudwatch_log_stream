# -*- mode: snippet -*-
# name: r_aws_cloudwatch_log_stream
# key: r_aws_cloudwatch_log_stream
# --

resource "aws_cloudwatch_log_stream" "${1:this}" {
    log_group_name = "${2:log_group_name}"
    name = "${3:name}"
}