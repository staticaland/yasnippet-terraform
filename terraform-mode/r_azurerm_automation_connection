# -*- mode: snippet -*-
# name: r_azurerm_automation_connection
# key: r_azurerm_automation_connection
# --

resource "azurerm_automation_connection" "${1:this}" {
    automation_account_name = "${2:automation_account_name}"
    name = "${3:name}"
    resource_group_name = "${4:resource_group_name}"
    type = "${5:type}"
    values = "${6:values}"
}