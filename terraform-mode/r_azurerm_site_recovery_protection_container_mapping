# -*- mode: snippet -*-
# name: r_azurerm_site_recovery_protection_container_mapping
# key: r_azurerm_site_recovery_protection_container_mapping
# --

resource "azurerm_site_recovery_protection_container_mapping" "${1:this}" {
    name = "${2:name}"
    recovery_fabric_name = "${3:recovery_fabric_name}"
    recovery_replication_policy_id = "${4:recovery_replication_policy_id}"
    recovery_source_protection_container_name = "${5:recovery_source_protection_container_name}"
    recovery_target_protection_container_id = "${6:recovery_target_protection_container_id}"
    recovery_vault_name = "${7:recovery_vault_name}"
    resource_group_name = "${8:resource_group_name}"
}