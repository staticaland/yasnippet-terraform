# -*- mode: snippet -*-
# name: r_opc_compute_route
# key: r_opc_compute_route
# --

resource "opc_compute_route" "${1:this}" {
    ip_address_prefix = "${2:ip_address_prefix}"
    name = "${3:name}"
    next_hop_vnic_set = "${4:next_hop_vnic_set}"
}