# -*- mode: snippet -*-
# name: r_flexibleengine_sdrs_replication_attach_v1
# key: r_flexibleengine_sdrs_replication_attach_v1
# --

resource "flexibleengine_sdrs_replication_attach_v1" "${1:this}" {
    device = "${2:device}"
    instance_id = "${3:instance_id}"
    replication_id = "${4:replication_id}"
}