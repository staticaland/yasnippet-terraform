# -*- mode: snippet -*-
# name: r_flexibleengine_lb_member_v2
# key: r_flexibleengine_lb_member_v2
# --

resource "flexibleengine_lb_member_v2" "${1:this}" {
    address = "${2:address}"
    pool_id = "${3:pool_id}"
    protocol_port = "${4:protocol_port}"
    subnet_id = "${5:subnet_id}"
}